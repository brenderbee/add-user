import constants from './../constants';
const { c } = constants;

function userListReducer(state = {}, action) {
  switch(action.type) {
    default:
      return state;
  }
}

export default userListReducer;
